[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn app.server:app --host 0.0.0.0 --port 8000"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[env]
PYTHON_VERSION = "3.11"
ENVIRONMENT = "production"
PORT = "8000"
